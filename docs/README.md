# shellquote docs

- `PRD.md` describes product scope.
- `TASKS.md` tracks factory delivery.
- `ORCHESTRATION.md` and `orchestration.json` capture the generated run plan.

See the root README for CLI usage.
